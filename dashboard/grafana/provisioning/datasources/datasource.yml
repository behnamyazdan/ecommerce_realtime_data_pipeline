apiVersion: 1

datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    uid: ecommerce_default_datasource
    jsonData:
      defaultDatabase: default
      port: 9000
      host: clickhouse
      username: default
      tlsSkipVerify: false

